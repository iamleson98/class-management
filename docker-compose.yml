# LMS system — production-style deployment.
#
# Services:
#   postgres     backend database
#   minio        S3-compatible object store for file uploads
#   minio-init   one-shot: creates the upload bucket
#   lms-server   Go backend (api :8065, metrics :8067)
#   lms-fe       Next.js frontend (:3000), proxies /api/v4/* -> lms-server
#   dev-proxy    Caddy reverse proxy (:80) — single origin for REST + WS (chat)
#   prometheus   scrapes lms-server metrics
#   grafana      dashboards (read from prometheus)
#
# Configure via the .env file (see .env.example).
# Start:   docker compose up -d
# Logs:    docker compose logs -f lms-server
# Stop:    docker compose down

name: lms

services:
  # ─────────────────────────── Database ───────────────────────────
  postgres:
    image: postgres:14-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mmuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-mattermost}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-mmuser} -d ${POSTGRES_DB:-mattermost}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lms-net

  # ─────────────────────── Object storage (S3) ────────────────────
  minio:
    image: minio/minio:RELEASE.2024-06-22T05-26-45Z
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?Set MINIO_ROOT_PASSWORD in .env}
    volumes:
      - miniodata:/data
    ports:
      # MinIO console (object browser) — restrict/remove in hardened prod
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - lms-net

  # One-shot bucket creator. Exits 0 when done.
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER:-minioadmin} ${MINIO_ROOT_PASSWORD};
      mc mb --ignore-existing local/${MM_FILESETTINGS_AMAZONS3_BUCKET:-lms-uploads};
      mc anonymous set download local/${MM_FILESETTINGS_AMAZONS3_BUCKET:-lms-uploads} || true;
      exit 0;
      "
    networks:
      - lms-net

  # ─────────────────────────── Backend ────────────────────────────
  lms-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      # ── Database ──
      MM_SQLSETTINGS_DRIVERNAME: postgres
      MM_SQLSETTINGS_DATASOURCE: ${MM_SQLSETTINGS_DATASOURCE:?Set MM_SQLSETTINGS_DATASOURCE in .env}

      # ── File storage -> MinIO (S3-compatible) ──
      MM_FILESETTINGS_DRIVERNAME: amazons3
      MM_FILESETTINGS_AMAZONS3ACCESSKEYID: ${MINIO_ROOT_USER:-minioadmin}
      MM_FILESETTINGS_AMAZONS3SECRETACCESSKEY: ${MINIO_ROOT_PASSWORD}
      MM_FILESETTINGS_AMAZONS3BUCKET: ${MM_FILESETTINGS_AMAZONS3_BUCKET:-lms-uploads}
      MM_FILESETTINGS_AMAZONS3ENDPOINT: http://minio:9000
      MM_FILESETTINGS_AMAZONS3SSL: "false"
      MM_FILESETTINGS_AMAZONS3REGION: us-east-1

      # ── Metrics ──
      MM_METRICSSETTINGS_ENABLE: "true"
      MM_METRICSSETTINGS_LISTENADDRESS: ":8067"

      # ── Misc ──
      MM_SERVICESETTINGS_SITEURL: ${MM_SERVICESETTINGS_SITEURL:-http://localhost:8065}
      MM_SERVICESETTINGS_ENABLELOCALMODE: "true"
      MM_SERVICESETTINGS_ALLOWCORSFROM: ${MM_SERVICESETTINGS_ALLOWCORSFROM:-http://localhost:3001}
      MM_SERVICESETTINGS_ALLOWEDUNTRUSTEDINTERNALCONNECTIONS: "minio"
      MM_NO_DOCKER: "true"
      MM_INSTALL_TYPE: docker
      TZ: ${TZ:-UTC}
    volumes:
      - mmdata:/mattermost/data
      - mmlogs:/mattermost/logs
      - mmconfig:/mattermost/config
    ports:
      - "${BACKEND_PORT:-8065}:8065"
    expose:
      - "8067" # metrics (scraped by prometheus on the internal network)
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8065/api/v4/system/ping"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3
    networks:
      - lms-net

  # ─────────────────────────── Frontend ───────────────────────────
  lms-fe:
    build:
      context: ./lms-fe
      dockerfile: Dockerfile
      args:
        # Baked into the client bundle at build time. On the compose network
        # the frontend reaches the backend through the proxy (same-origin), so
        # this points at the backend service name.
        NEXT_PUBLIC_API_URL: http://lms-server:8065
    restart: unless-stopped
    depends_on:
      lms-server:
        condition: service_healthy
    environment:
      NODE_ENV: production
      # Used by server-side rewrites/proxy at runtime.
      NEXT_PUBLIC_API_URL: http://lms-server:8065
      TZ: ${TZ:-UTC}
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    networks:
      - lms-net

  # ───────────────────────── Dev reverse proxy ──────────────────────
  # Caddy serves the whole app on a single origin (http://localhost) so that
  # the httpOnly MMAUTHTOKEN cookie is first-party for BOTH REST and the
  # WebSocket (/api/v4/websocket). This mirrors the prod Traefik path rule
  # and makes real-time chat work in dev without CORS/cookie gymnastics.
  # Use http://localhost (PROXY_PORT) for local dev; the browser then talks
  # same-origin for everything and NEXT_PUBLIC_API_URL is unused client-side.
  # Path routing rules live in deploy/caddy/Caddyfile.
  dev-proxy:
    image: caddy:2.8-alpine
    restart: unless-stopped
    depends_on:
      - lms-fe
      - lms-server
    ports:
      - "${PROXY_PORT:-80}:80"
    volumes:
      - ./deploy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    networks:
      - lms-net

  # ─────────────────────── Observability ──────────────────────────
  prometheus:
    image: prom/prometheus:v2.54.1
    restart: unless-stopped
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - promdata:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"
      - "--web.enable-lifecycle"
    expose:
      - "9090"
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    depends_on:
      - lms-server
    networks:
      - lms-net

  grafana:
    image: grafana/grafana:10.4.2
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?Set GRAFANA_ADMIN_PASSWORD in .env}
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      # Auto-provision Prometheus datasource + dashboard provider
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      # Drop dashboard JSON files here to have them auto-loaded
      - grafana-dashboards:/var/lib/grafana/dashboards
      - grafana-data:/var/lib/grafana
    ports:
      # 3001 to avoid clashing with the frontend on 3000
      - "${GRAFANA_PORT:-3001}:3000"
    depends_on:
      - prometheus
    networks:
      - lms-net

networks:
  lms-net:
    driver: bridge

volumes:
  pgdata:
  miniodata:
  mmdata:
  mmlogs:
  mmconfig:
  promdata:
  grafana-data:
  grafana-dashboards:
