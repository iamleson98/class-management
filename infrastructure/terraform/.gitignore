# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example

# Fetched Swarm join token (contains cluster secret)
.swarm-worker-token

# Local plans
*.tfplan
