# This file is auto-generated by scripts/generate-lms-openapi.js.
# It reads route registrations from Go code and metadata from lms_api_manifest.json.
# To update: edit the manifest and/or Go code, then re-run the generator.
  /api/v4/lms/banners:
    get:
      tags:
        - lms
      summary: Get banners
      description: |
        Get all banners.
        ##### Permissions
        `lms_manage_banners`
      operationId: LmsGetBanners
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsBanner"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create a banner
      description: |
        Create a new banner.
        ##### Permissions
        `lms_manage_banners`
      operationId: LmsPostBanners
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBanner"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBanner"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/banners/{id}:
    get:
      tags:
        - lms
      summary: Get a banner
      description: |
        Get a banner by ID.
        ##### Permissions
        `lms_manage_banners`
      operationId: LmsGetBannersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBanner"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a banner
      description: |
        Update a banner by ID.
        ##### Permissions
        `lms_manage_banners`
      operationId: LmsPutBannersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBanner"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBanner"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a banner
      description: |
        Delete a banner by ID.
        ##### Permissions
        `lms_manage_banners`
      operationId: LmsDeleteBannersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/branches:
    post:
      tags:
        - lms
      summary: Get branches
      description: |
        Get a filtered list of branches. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_branches`
      operationId: LmsPostBranches
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBranchFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsBranch"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/branches/create:
    post:
      tags:
        - lms
      summary: Create a branch
      description: |
        Create a new branch.
        ##### Permissions
        `lms_manage_branches`
      operationId: LmsPostBranchesCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBranch"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBranch"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/class-media:
    post:
      tags:
        - lms
      summary: Create class media
      description: |
        Upload a new class media (photo or video).
        ##### Permissions
        `lms_manage_class_media`
      operationId: LmsPostClassMedia
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsClassMedium"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsClassMedium"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/class-media/search:
    post:
      tags:
        - lms
      summary: Get class media
      description: |
        Get a filtered list of class media (photos/videos). Uses POST body for filter options.
        ##### Permissions
        `lms_manage_class_media`
      operationId: LmsPostClassMediaSearch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsClassMediaFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsClassMedium"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/class-media/{id}:
    delete:
      tags:
        - lms
      summary: Delete class media
      description: |
        Delete a class media by ID.
        ##### Permissions
        `lms_manage_class_media`
      operationId: LmsDeleteClassMediaId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/classes:
    post:
      tags:
        - lms
      summary: Get classes
      description: |
        Get a filtered list of classes. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsPostClasses
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsClassFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsClass"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/classes/create:
    post:
      tags:
        - lms
      summary: Create a class
      description: |
        Create a new class.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsPostClassesCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsClass"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsClass"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/classes/{id}:
    get:
      tags:
        - lms
      summary: Get a class
      description: |
        Get a class by ID.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsGetClassesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsClass"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a class
      description: |
        Update a class by ID.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsPutClassesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsClass"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsClass"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a class
      description: |
        Delete a class by ID.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsDeleteClassesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/classes/{id}/enroll:
    post:
      tags:
        - lms
      summary: Enroll students in a class
      description: |
        Enroll a list of students into the specified class.
        ##### Permissions
        `lms_manage_classes`
      operationId: LmsPostClassesIdEnroll
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsEnrollStudentsRequest"
      responses:
        "201":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/courses:
    get:
      tags:
        - lms
      summary: Get courses
      description: |
        Get a list of all courses.
      operationId: LmsGetCourses
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsCourse"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create a course
      description: |
        Create a new course.
        ##### Permissions
        `lms_manage_courses`
      operationId: LmsPostCourses
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsCourse"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsCourse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/courses/{id}:
    get:
      tags:
        - lms
      summary: Get a course
      description: |
        Get a course by ID.
        ##### Permissions
        `lms_manage_courses`
      operationId: LmsGetCoursesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsCourse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a course
      description: |
        Update a course by ID.
        ##### Permissions
        `lms_manage_courses`
      operationId: LmsPutCoursesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsCourse"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsCourse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a course
      description: |
        Delete a course by ID.
        ##### Permissions
        `lms_manage_courses`
      operationId: LmsDeleteCoursesId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/dashboard:
    get:
      tags:
        - lms
      summary: Get dashboard stats
      description: |
        Get dashboard statistics and summary data.
        ##### Permissions
        `lms_manage_dashboard`
      operationId: LmsGetDashboard
      responses:
        "200":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/fee-packages:
    post:
      tags:
        - lms
      summary: Get fee packages
      description: |
        Get a filtered list of fee packages. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_fee_packages`
      operationId: LmsPostFeePackages
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsFeePackageFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsFeePackage"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/fee-packages/create:
    post:
      tags:
        - lms
      summary: Create a fee package
      description: |
        Create a new fee package.
        ##### Permissions
        `lms_manage_fee_packages`
      operationId: LmsPostFeePackagesCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsFeePackage"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsFeePackage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/homeworks:
    post:
      tags:
        - lms
      summary: Get homeworks
      description: |
        Get a filtered list of homeworks. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsPostHomeworks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsHomeworkFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsHomework"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/homeworks/bulk-assign:
    post:
      tags:
        - lms
      summary: Bulk assign homework
      description: |
        Create a homework and assign it to multiple students at once.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsPostHomeworksBulkAssign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBulkAssignHomeworkRequest"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBulkAssignHomeworkResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/homeworks/create:
    post:
      tags:
        - lms
      summary: Create a homework
      description: |
        Create a new homework assignment.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsPostHomeworksCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsHomework"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsHomework"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/homeworks/{id}:
    get:
      tags:
        - lms
      summary: Get a homework
      description: |
        Get a homework by ID.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsGetHomeworksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsHomework"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a homework
      description: |
        Update a homework by ID.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsPutHomeworksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsHomework"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsHomework"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a homework
      description: |
        Delete a homework by ID.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsDeleteHomeworksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/homeworks/{id}/submissions:
    get:
      tags:
        - lms
      summary: Get homework submissions
      description: |
        Get submissions for a homework assignment.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsGetHomeworksIdSubmissions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Upsert a homework submission
      description: |
        Create or update a homework submission.
        ##### Permissions
        `lms_manage_homework`
      operationId: LmsPostHomeworksIdSubmissions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsSubmission"
      responses:
        "201":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/leads:
    post:
      tags:
        - lms
      summary: Get leads
      description: |
        Get a filtered list of leads. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsPostLeads
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsLeadFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsLead"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/leads/create:
    post:
      tags:
        - lms
      summary: Create a lead
      description: |
        Create a new lead.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsPostLeadsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsLead"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/leads/{id}:
    get:
      tags:
        - lms
      summary: Get a lead
      description: |
        Get a lead by ID.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsGetLeadsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a lead
      description: |
        Update a lead by ID.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsPutLeadsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsLead"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a lead
      description: |
        Delete a lead by ID.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsDeleteLeadsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/leads/{id}/activities:
    get:
      tags:
        - lms
      summary: Get lead activities
      description: |
        Get activities for a lead.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsGetLeadsIdActivities
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LmsLeadActivity"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create a lead activity
      description: |
        Create an activity for a lead.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsPostLeadsIdActivities
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsLeadActivity"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsLeadActivity"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/leads/{id}/convert:
    post:
      tags:
        - lms
      summary: Convert lead to student
      description: |
        Convert a lead into a student.
        ##### Permissions
        `lms_manage_leads`
      operationId: LmsPostLeadsIdConvert
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsConvertLeadResponse"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/materials:
    post:
      tags:
        - lms
      summary: Get materials
      description: |
        Get a filtered list of study materials. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_materials`
      operationId: LmsPostMaterials
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsMaterialFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsMaterial"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/materials/create:
    post:
      tags:
        - lms
      summary: Create a material
      description: |
        Create a new study material.
        ##### Permissions
        `lms_manage_materials`
      operationId: LmsPostMaterialsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsMaterial"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsMaterial"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/materials/{id}:
    get:
      tags:
        - lms
      summary: Get a material
      description: |
        Get a material by ID.
        ##### Permissions
        `lms_manage_materials`
      operationId: LmsGetMaterialsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsMaterial"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a material
      description: |
        Update a material by ID.
        ##### Permissions
        `lms_manage_materials`
      operationId: LmsPutMaterialsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsMaterial"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsMaterial"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a material
      description: |
        Delete a material by ID.
        ##### Permissions
        `lms_manage_materials`
      operationId: LmsDeleteMaterialsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/notifications:
    get:
      tags:
        - lms
      summary: Get notifications
      description: |
        Get notifications for the current user.
        ##### Permissions
        `lms_view_notifications`
      operationId: LmsGetNotifications
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsNotificationListResponse"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/notifications/{id}/read:
    post:
      tags:
        - lms
      summary: Mark notification as read
      description: |
        Mark a notification as read.
        ##### Permissions
        `lms_view_notifications`
      operationId: LmsPostNotificationsIdRead
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/payments:
    post:
      tags:
        - lms
      summary: Get payments
      description: |
        Get a filtered list of payments. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsPostPayments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsPaymentFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsPayment"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/posts:
    post:
      tags:
        - lms
      summary: Get blog posts
      description: |
        Get a filtered list of blog posts. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsPostPosts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBlogPostFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsBlogPost"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/posts/categories:
    get:
      tags:
        - lms
      summary: Get post categories
      description: |
        Get all post categories.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsGetPostsCategories
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsPostCategory"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create a post category
      description: |
        Create a new post category.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsPostPostsCategories
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsPostCategory"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsPostCategory"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/posts/create:
    post:
      tags:
        - lms
      summary: Create a blog post
      description: |
        Create a new blog post.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsPostPostsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBlogPost"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/posts/{id}:
    get:
      tags:
        - lms
      summary: Get a blog post
      description: |
        Get a blog post by ID.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsGetPostsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a blog post
      description: |
        Update a blog post by ID.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsPutPostsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsBlogPost"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsBlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a blog post
      description: |
        Delete a blog post by ID.
        ##### Permissions
        `lms_manage_posts`
      operationId: LmsDeletePostsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/public/contact:
    post:
      tags:
        - lms
      summary: Submit contact form
      description: |
        Submit a public contact/inquiry form. No authentication required.
      operationId: LmsPostPublicContact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsPublicContactRequest"
      responses:
        "201":
          description: Success.
  /api/v4/lms/public/courses:
    get:
      tags:
        - lms
      summary: Get public courses
      description: |
        Returns all published courses. No authentication required.
      operationId: LmsGetPublicCourses
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsCourse"
                  total_count:
                    type: integer
                    format: int64
  /api/v4/lms/public/forgot-password:
    post:
      tags:
        - lms
      summary: Request password reset
      description: |
        Send a password reset email. No authentication required.
      operationId: LmsPostPublicForgotPassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsForgotPasswordRequest"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsSuccessMessage"
  /api/v4/lms/public/posts:
    get:
      tags:
        - lms
      summary: Get public blog posts
      description: |
        Returns all published blog posts. No authentication required.
      operationId: LmsGetPublicPosts
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsBlogPost"
                  total_count:
                    type: integer
                    format: int64
  /api/v4/lms/public/register:
    post:
      tags:
        - lms
      summary: Public lead registration
      description: |
        Register a new lead via the public form. No authentication required.
      operationId: LmsPostPublicRegister
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsLead"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsLead"
  /api/v4/lms/public/reset-password:
    post:
      tags:
        - lms
      summary: Reset password
      description: |
        Reset password using a token. No authentication required.
      operationId: LmsPostPublicResetPassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsResetPasswordRequest"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsSuccessMessage"
  /api/v4/lms/public/verify-token:
    get:
      tags:
        - lms
      summary: Verify password reset token
      description: |
        Check if a password reset token is valid. No authentication required.
      operationId: LmsGetPublicVerifyToken
      parameters:
        - in: query
          name: token
          required: true
          description: The password reset token
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsVerifyTokenResponse"
  /api/v4/lms/reports:
    get:
      tags:
        - lms
      summary: Get a report
      description: |
        Get a report by type. The response structure varies based on the report type.
        ##### Permissions
        `lms_view_reports`
      operationId: LmsGetReports
      parameters:
        - in: query
          name: type
          required: true
          description: The type of report to generate
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/sessions:
    post:
      tags:
        - lms
      summary: Get sessions
      description: |
        Get a filtered list of class sessions. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_sessions`
      operationId: LmsPostSessions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsSessionFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsSession"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/sessions/create:
    post:
      tags:
        - lms
      summary: Create a session
      description: |
        Create a new class session.
        ##### Permissions
        `lms_manage_sessions`
      operationId: LmsPostSessionsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsSession"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsSession"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/sessions/{id}:
    get:
      tags:
        - lms
      summary: Get a session
      description: |
        Get a session by ID.
        ##### Permissions
        `lms_manage_sessions`
      operationId: LmsGetSessionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsSession"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a session
      description: |
        Update a session by ID.
        ##### Permissions
        `lms_manage_sessions`
      operationId: LmsPutSessionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsSession"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsSession"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a session
      description: |
        Delete a session by ID.
        ##### Permissions
        `lms_manage_sessions`
      operationId: LmsDeleteSessionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsDataTrue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/sessions/{id}/attendance:
    get:
      tags:
        - lms
      summary: Get session attendance
      description: |
        Get attendance records for a session.
        ##### Permissions
        `lms_manage_attendance`
      operationId: LmsGetSessionsIdAttendance
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Save session attendance
      description: |
        Save attendance records for a session.
        ##### Permissions
        `lms_manage_attendance`
      operationId: LmsPostSessionsIdAttendance
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/LmsAttendance"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/students:
    post:
      tags:
        - lms
      summary: Get students
      description: |
        Get a filtered list of students. Uses POST body for filter options instead of query parameters.
        ##### Permissions
        `lms_manage_students`
      operationId: LmsPostStudents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsStudentFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsResponseList"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/students/create:
    post:
      tags:
        - lms
      summary: Create a student
      description: |
        Create a new student with user account and student properties.
        ##### Permissions
        `lms_manage_students`
      operationId: LmsPostStudentsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsStudent"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsStudent"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/students/{id}:
    get:
      tags:
        - lms
      summary: Get a student
      description: |
        Get a student by ID.
        ##### Permissions
        `lms_manage_students`
      operationId: LmsGetStudentsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsStudent"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a student
      description: |
        Update a student by ID.
        ##### Permissions
        `lms_manage_students`
      operationId: LmsPutStudentsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsStudent"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsStudent"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a student
      description: |
        Delete a student by ID.
        ##### Permissions
        `lms_manage_students`
      operationId: LmsDeleteStudentsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsDataTrue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/tasks:
    post:
      tags:
        - lms
      summary: Get tasks
      description: |
        Get a filtered list of tasks. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_tasks`
      operationId: LmsPostTasks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTaskFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsTask"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/tasks/create:
    post:
      tags:
        - lms
      summary: Create a task
      description: |
        Create a new task.
        ##### Permissions
        `lms_manage_tasks`
      operationId: LmsPostTasksCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTask"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsTask"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/tasks/{id}:
    get:
      tags:
        - lms
      summary: Get a task
      description: |
        Get a task by ID.
        ##### Permissions
        `lms_manage_tasks`
      operationId: LmsGetTasksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsTask"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a task
      description: |
        Update a task by ID.
        ##### Permissions
        `lms_manage_tasks`
      operationId: LmsPutTasksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTask"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsTask"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a task
      description: |
        Delete a task by ID.
        ##### Permissions
        `lms_manage_tasks`
      operationId: LmsDeleteTasksId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsDataTrue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/tuitions:
    post:
      tags:
        - lms
      summary: Get tuitions
      description: |
        Get a filtered list of tuitions. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsPostTuitions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTuitionFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsTuition"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/tuitions/create:
    post:
      tags:
        - lms
      summary: Create a tuition
      description: |
        Create a new tuition record.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsPostTuitionsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTuition"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsTuition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/tuitions/{id}:
    get:
      tags:
        - lms
      summary: Get a tuition
      description: |
        Get a tuition by ID.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsGetTuitionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsTuition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a tuition
      description: |
        Update a tuition by ID.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsPutTuitionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsTuition"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsTuition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a tuition
      description: |
        Delete a tuition by ID.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsDeleteTuitionsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsStatusOK"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/tuitions/{id}/payments:
    get:
      tags:
        - lms
      summary: Get tuition payments
      description: |
        Get payments for a specific tuition.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsGetTuitionsIdPayments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create a tuition payment
      description: |
        Create a payment for a specific tuition. The tuition_id is set from the URL path.
        ##### Permissions
        `lms_manage_tuition`
      operationId: LmsPostTuitionsIdPayments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsPayment"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsPayment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/users:
    get:
      tags:
        - lms
      summary: Get LMS users
      description: |
        Get a list of LMS users (staff/admin).
        ##### Permissions
        `lms_manage_users`
      operationId: LmsGetUsers
      parameters:
        - in: query
          name: role
          description: Filter by user role
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags:
        - lms
      summary: Create an LMS user
      description: |
        Create a new LMS user (staff/admin).
        ##### Permissions
        `lms_manage_users`
      operationId: LmsPostUsers
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/User"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/users/{id}:
    get:
      tags:
        - lms
      summary: Get an LMS user
      description: |
        Get an LMS user by ID.
        ##### Permissions
        `lms_manage_users`
      operationId: LmsGetUsersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update an LMS user
      description: |
        Update an LMS user by ID.
        ##### Permissions
        `lms_manage_users`
      operationId: LmsPutUsersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/User"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete an LMS user
      description: |
        Delete an LMS user by ID.
        ##### Permissions
        `lms_manage_users`
      operationId: LmsDeleteUsersId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsDataTrue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v4/lms/weekly-reviews:
    post:
      tags:
        - lms
      summary: Get weekly reviews
      description: |
        Get a filtered list of weekly reviews. Uses POST body for filter options.
        ##### Permissions
        `lms_manage_weekly_reviews`
      operationId: LmsPostWeeklyReviews
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsWeeklyReviewFilterOpts"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/LmsWeeklyReview"
                  total_count:
                    type: integer
                    format: int64
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/weekly-reviews/create:
    post:
      tags:
        - lms
      summary: Create a weekly review
      description: |
        Create a new weekly review.
        ##### Permissions
        `lms_manage_weekly_reviews`
      operationId: LmsPostWeeklyReviewsCreate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsWeeklyReview"
      responses:
        "201":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsWeeklyReview"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v4/lms/weekly-reviews/{id}:
    get:
      tags:
        - lms
      summary: Get a weekly review
      description: |
        Get a weekly review by ID.
        ##### Permissions
        `lms_manage_weekly_reviews`
      operationId: LmsGetWeeklyReviewsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsWeeklyReview"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - lms
      summary: Update a weekly review
      description: |
        Update a weekly review by ID.
        ##### Permissions
        `lms_manage_weekly_reviews`
      operationId: LmsPutWeeklyReviewsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LmsWeeklyReview"
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LmsWeeklyReview"
                  error:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - lms
      summary: Delete a weekly review
      description: |
        Delete a weekly review by ID.
        ##### Permissions
        `lms_manage_weekly_reviews`
      operationId: LmsDeleteWeeklyReviewsId
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LmsDataTrue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
